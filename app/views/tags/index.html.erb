<h1 class="pageTitle">
  Browse Tags
</h1>

<div class="tag_index">
  <ul>
  <% @tags.each do |tag| %>
    <p><%= link_to tag.tag_name, tag_path(tag) %> Photos: <%=tag.photos.count%></p>
  <% end %>
  </ul>
</div>
