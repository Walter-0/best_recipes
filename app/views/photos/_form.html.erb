<%= form_for(@photo) do |f| %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :photo_url %>
<%= f.text_field :photo_url %>

<%= f.label :user_id %>
<%= f.text_field :user_id%>
<!-- prepopulate this field with the current user id -->

<%= f.submit%>

<% end %>
